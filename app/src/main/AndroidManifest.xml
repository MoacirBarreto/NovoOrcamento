<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">

    <!-- CORREÇÃO DO ERRO: Declara que o app não exige recursos de Android TV -->
    <!-- Adicione esta linha logo após a abertura da tag <manifest> -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lume"
        tools:targetApi="31">

        <!--
            Activity Principal (Launcher):
            windowSoftInputMode="adjustPan" impede que o teclado empurre os gráficos
            para cima de forma desordenada na MainActivity.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Activity de Novo Lançamento:
            windowSoftInputMode="adjustResize" garante que o layout se ajuste
            quando o teclado abrir, mantendo o botão "Salvar" visível.
        -->
        <activity
            android:name=".NovoLancamentoActivity"
            android:exported="false"
            android:label="Novo Lançamento"
            android:windowSoftInputMode="adjustResize" />

    </application>
</manifest>